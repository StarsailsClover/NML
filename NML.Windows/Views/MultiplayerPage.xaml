<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NML.Windows.Views.MultiplayerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock Text="多人游戏" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="P2P联机 - 无需公网IP" Opacity="0.6"/>
        </StackPanel>

        <!-- Status -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <InfoBadge x:Name="StatusBadge" Value="在线"/>
            <TextBlock x:Name="NodeText" Text="Node ID: ..." Opacity="0.6"/>
            <TextBlock x:Name="LatencyText" Text="延迟: --ms" Opacity="0.6"/>
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <Button x:Name="HostButton" Click="HostButton_Click" Style="{ThemeResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="AddFriend"/>
                    <TextBlock Text="创建世界" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>

            <Button x:Name="RefreshButton" Click="RefreshButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Refresh"/>
                    <TextBlock Text="刷新"/>
                </StackPanel>
            </Button>

            <ToggleSwitch x:Name="AutoDiscoverToggle" Header="自动发现" IsOn="True"/>
        </StackPanel>

        <!-- World List -->
        <ListView Grid.Row="3" x:Name="WorldsList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <SymbolIcon Grid.Column="0" Symbol="World" FontSize="32"/>

                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Text="{Binding WorldName}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Motd}" FontSize="12" Opacity="0.6" TextTrimming="CharacterEllipsis"/>
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <TextBlock Text="{Binding PlayerCount}" FontSize="12" Opacity="0.8"/>
                                <TextBlock Text="{Binding LatencyMs}" FontSize="12" Opacity="0.6"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="2" Content="加入" Style="{ThemeResource AccentButtonStyle}" Click="JoinButton_Click"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

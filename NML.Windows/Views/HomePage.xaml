<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NML.Windows.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock Text="NML" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="选择版本开始游戏" Opacity="0.6"/>
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <Button x:Name="LaunchButton" Click="LaunchButton_Click" Style="{ThemeResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Play"/>
                    <TextBlock Text="启动游戏" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>

            <DropDownButton Content="安装新版本">
                <DropDownButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="正式版" Click="InstallRelease_Click"/>
                        <MenuFlyoutItem Text="快照版" Click="InstallSnapshot_Click"/>
                        <MenuFlyoutItem Text="Forge" Click="InstallForge_Click"/>
                        <MenuFlyoutItem Text="Fabric" Click="InstallFabric_Click"/>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
        </StackPanel>

        <!-- Version List -->
        <GridView Grid.Row="2" x:Name="VersionsGridView" SelectionChanged="VersionsGridView_SelectionChanged">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="280" Padding="16" RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Video" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                            <TextBlock Text="{Binding Id}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Spacing="4">
                            <TextBlock Text="{Binding Type}" FontSize="12" Opacity="0.6"/>
                            <TextBlock Text="{Binding ReleaseTime}" FontSize="12" Opacity="0.6"/>
                        </StackPanel>

                        <Button Grid.Row="2" Content="启动" Style="{ThemeResource AccentButtonStyle}" Click="LaunchVersionButton_Click"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
